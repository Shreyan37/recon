// This is a comment (slightly modified)
/// This is a doc comment for the function (updated).
fn foo() {
    // inside function (now with more text)
    println!("foo");
    // added trailing comment
}
